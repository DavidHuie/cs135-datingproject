package login_utilities;

public class UserAuthentication {
	public static Boolean authenticate_login(String username, String password) {
		if (username == null || password == null) {
			return false;
		}
		
		// Check login credentials in database
		
		return true;
	}
}
